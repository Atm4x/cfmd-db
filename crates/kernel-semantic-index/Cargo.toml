[package]
name = "kernel-semantic-index"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
kernel-schema = { path = "../kernel-schema" }
kernel-types = { path = "../kernel-types" }

[lints]
workspace = true
