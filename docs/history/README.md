# Historical Archive

This directory preserves the append-only R&D lineage that previously occupied the repository root.

Historical files are provenance/evidence, not the preferred current documentation. Start from the root `README.md`, `SPEC.md`, `docs/architecture/ARCHITECTURE.md` and `docs/status/PROJECT_STATUS.md`.

Do not rewrite old pass reports to match newer terminology; newer corrections belong in current docs or a new report.
